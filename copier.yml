platform:
  choices:
    GITHUB: "github"
    GITLAB: "gitlab"
  default: "github"
  type: str

use_semver:
  type: bool
  help: Do you want to use semnatic versioning?
  default: yes

use_renovate:
  type: bool
  help: Do you want to use renovate?
  default: yes

type:
  choices:
    DJANGO: "django"
    NODEJS: "nodejs"
    PYTHON: "python"
    R: "r"
  default: "django"
  type: str

_exclude:
  - /env
  - /.git
  - /copier.yml
  - README.md
